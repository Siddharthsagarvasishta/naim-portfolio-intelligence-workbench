# nAIM public companion intentionally requires no secrets in offline mode.
# Configure non-sensitive API origin values as environment variables instead:
# NAIM_PUBLIC_DEMO_MODE="API"
# NAIM_PUBLIC_API_BASE_URL="https://example.invalid/"
